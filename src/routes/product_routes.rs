use rocket::response::{content, status};

// use crate::controllers::product_controller::get_products;

// #[get("/products")]
// pub async fn get_products_route() -> status::Custom<content::Json<&'static str>> {
//     get_products().await.unwrap()
// }
